"use client";
import { useKioskStore } from "@/store/kiosk";
export function LiveProgressPill() { const liveProgress=useKioskStore((s)=>s.liveProgress); const debugEnabled=useKioskStore((s)=>s.debugEnabled); const dot=liveProgress.stability==="stable"?"bg-emerald-400":liveProgress.stability==="watching"?"bg-amber-300":"bg-rose-400"; return <div className="fixed bottom-4 left-4 z-30 flex items-center gap-3 rounded-full border border-white/10 bg-slate-950/80 px-4 py-2 text-xs text-white shadow-lg backdrop-blur"><span className={`size-2 rounded-full ${dot}`} /><span className="font-semibold uppercase tracking-[0.25em] text-white/50">Live build</span><span className="max-w-[32rem] truncate text-white/85">{liveProgress.label}</span>{debugEnabled?<span className="rounded-full bg-amber-300/15 px-2 py-1 font-semibold text-amber-100">DEBUG ON</span>:null}</div>; }
